# Replace with the existing network used by your SUB2API container.
SUB2API_NETWORK=your-sub2api-network
# Optional outbound proxy reachable from inside the sidecar, not the host loopback.
EXCEL_BRIDGE_PROXY=
